
@{
    ViewBag.Title = "ilanListele";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@using real_estate_web_application.Models;

<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                İlan Listesi
            </header>
            @{
                if (TempData["a"] != null)
                {
                    <div class="alert alert-danger alert-dismissible">
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                        <strong>@TempData["a"]</strong>
                    </div>
                }
            }
            <table class="table table-striped table-advance table-hover">
                <thead>
                    <tr>
                        <th><i class="icon-edit"></i>Başlık</th>
                        <th><i class="icon-edit"></i>Fiyat</th>
                        <th><i class="icon-edit"></i>Tarih</th>
                        <th><i class="icon-edit"></i>Açıklama</th>
                        <th><i class="icon-edit"></i>Durum</th>
                        <th><i class="icon-edit"></i>Kategori</th>
                        <th><i class="icon-edit"></i>İşlemler</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (Ilan item in ViewBag.ilanlar)
                    {
                            <tr>
                                <td>@item.baslik</td>
                                <td>@item.fiyat</td>
                                <td>@item.tarih</td>
                                <td>@item.aciklama</td>
                                <td>@item.durum</td>
                                <td>@item.Kategori.kategoriAdi</td>
                                <td>
                                    <button class="btn btn-primary btn-xs"><i class="icon-pencil"></i></button>
                                    <button class="btn btn-primary btn-xs"><i class="icon-pencil"></i></button>
                                    <a href="@Url.Action("konutSil" , new {item.ilanID})" class="btn btn-danger btn-xs" onclick="return confirmDel()"><i class="glyphicon glyphicon-remove"></i></a>
                                </td>
                            </tr>
                    }

                </tbody>
            </table>
        </section>
    </div>
</div>

<script type="text/javascript">
        function confirmDel() {
            var agree = confirm("Silmek İstiyor musunuz ?");
            if (agree) {
                return true;
            }
            else {
                return false;
            }
        }
</script>
